$('.js-sub-items').html("<%= escape_javascript(select_tag 'mainpages[itemtype_id]', options_for_select(@item_types.map { |item| [item.itemtypename, item.id] }), :class => 'form-control', :id => 'js-items' ) %>");